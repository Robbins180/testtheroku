<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <h1>Reindeer show page</h1>
        The <%=fruit.name; %> is <%=fruit.color; %>!
        <% if(fruit.readyToBuy === true) { %>
            It is ready to Buy.
        <% } else { %>
            It is not ready to Buy.
        <% } %>
        <nav>
            <a href="/fruits">Back to Fruits Index</a>
        </nav>
    </body>
</html>
